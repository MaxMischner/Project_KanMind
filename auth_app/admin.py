# Register your models here.
"""Admin configuration for auth_app."""

from django.contrib import admin

# User model is already registered by Django's built-in admin
# No additional models in auth_app need registration
